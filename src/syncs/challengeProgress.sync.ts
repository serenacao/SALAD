import {
  ChallengeDefinition,
  Requesting,
  Session,
  UserAuthentication,
  ChallengeProgress,
  ChallengeVerification,
} from "@concepts";
import { actions, Sync } from "@engine";
// uploadChallenge (should fire when a challenge is defined in ChallengeDefinition)

// removeChallenge (should fire when a challenge is removed in ChallengeDefinition)

// completePart (should fire when a part if verified in ChallengeVerification)
